// Defining types for the future use

export interface MovieCardType {
    title: string;
    imgUrl: string;
    _id: string;
    rating: number;
    type: string;
}
export interface celebCardTypes {
    _id: string;
    name: string;
    role: string;
    imageUrl: string;
}